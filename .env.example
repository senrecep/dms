# =============================================================================
# DMS - Document Management System
# =============================================================================

# Database (PostgreSQL)
DATABASE_URL=postgresql://dms:dms_password@localhost:5432/dms

# Redis
REDIS_URL=redis://localhost:6379

# Better Auth
BETTER_AUTH_SECRET=your-secret-key-min-32-chars-here
BETTER_AUTH_URL=http://localhost:3000

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=DMS

# File Storage
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=500

# Approval Settings (defaults, configurable via admin panel)
DEFAULT_REMINDER_DAYS=3
DEFAULT_ESCALATION_DAYS=7

# Cron job authentication
CRON_SECRET=dev-cron-secret-change-in-production

# Seed (Initial Admin)
SEED_ADMIN_NAME=System Admin
SEED_ADMIN_EMAIL=admin@dms.com
SEED_ADMIN_PASSWORD=change-this-password

# =============================================================================
# Email Configuration
# =============================================================================
# Email settings (provider, API keys, SMTP, sender address, language) are
# managed through the admin panel at /settings after first login.
# The seed script creates sensible defaults — no env vars needed.
# =============================================================================
